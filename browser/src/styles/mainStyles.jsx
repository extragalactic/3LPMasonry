const PaperStyles = {
  generics: {
    container: {},
    left: {
      width: window.innerWidth / 2,
      height: window.innerHeight / 1.3,
      margin: 20,
    },
    right: {
      width: window.innerWidth / 2,
      height: window.innerHeight / 1.3,
      margin: 20,
    },
  },
};

const CardStyles = {};



export { PaperStyles, CardStyles };
